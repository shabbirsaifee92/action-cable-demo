<h1> Authenticate as one of the following users to start chatting </h1>

<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.username.capitalize, session_path(user_id: user.id), method: :post %> </li>
  <% end %>
</ul>    
